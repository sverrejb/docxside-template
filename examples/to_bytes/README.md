# To Bytes

Generates a `.docx` from a template and returns it as a `Vec<u8>` in memory — useful for piping into other processing steps without touching the filesystem.

## Run

```bash
cargo run -p to-bytes
```
